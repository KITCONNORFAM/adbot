# ═══════════════════════════════════════════════════════
#  AdBot — Environment Variables Template
#  Copy to .env and fill in your values
# ═══════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────
# REQUIRED — Bot will NOT start without these
# ────────────────────────────────────────────────────────

# Telegram Bot Token (from @BotFather)
BOT_TOKEN=your_bot_token_here

# Telegram Bot username (without @)
BOT_USERNAME=your_bot_username

# Supabase project URL   (Settings → API → Project URL)
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/service key (Settings → API → anon key)
SUPABASE_KEY=your_supabase_key_here

# Encryption key for Telethon session strings (generate once, NEVER change after first use)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_key_here

# ────────────────────────────────────────────────────────
# OWNER BOOTSTRAP — Seed initial owner IDs on first launch
# ────────────────────────────────────────────────────────

# Comma-separated Telegram user IDs for initial owners
# Only needed on FIRST deployment. Owners persist in DB after that.
INITIAL_OWNER_IDS=123456789,987654321

# ────────────────────────────────────────────────────────
# OPTIONAL — Bot will use defaults if not set
# ────────────────────────────────────────────────────────

# Start message image URL (shown on /start)
START_IMAGE_URL=https://your-image-url.jpg

# Support/contact username shown to non-premium users (without @)
SUPPORT_USERNAME=your_support_username

# ────────────────────────────────────────────────────────
# FEATURE CONFIG — Defaults are production-ready
# ────────────────────────────────────────────────────────

# Free trial duration in days  [default: 15]
TRIAL_DAYS=15

# Referrals required to earn reward  [default: 30]
REFERRALS_REQUIRED=30

# Premium days awarded for referral milestone  [default: 14]
REFERRAL_REWARD_DAYS=14

# Default premium days when owner uses /addprem with no days arg  [default: 30]
PREMIUM_DEFAULT_DAYS=30

# ────────────────────────────────────────────────────────
# SERVER CONFIG — Required for VPS webhook/health endpoint
# ────────────────────────────────────────────────────────

# Port for health-check server  [default: 8080]
PORT=8080

# Webhook URL (only if using webhook mode, not polling)
# Leave blank to use long polling (recommended for VPS)
# WEBHOOK_URL=https://your-domain.com/webhook

# ────────────────────────────────────────────────────────
# INTERNALS — Tuning (defaults are fine for most setups)
# ────────────────────────────────────────────────────────

# Seconds to wait before retrying after network error  [default: 3]
RETRY_DELAY=3

# Scheduler cron check interval in seconds  [default: 300 = 5 min]
SCHEDULER_INTERVAL=300

# Logging level  [default: INFO]  Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ────────────────────────────────────────────────────────
# NOTES
# ────────────────────────────────────────────────────────
# 1. Never commit .env to git — add it to .gitignore
# 2. ENCRYPTION_KEY must NEVER change after accounts are stored in DB
#    (if changed, all stored session strings become unreadable)
# 3. INITIAL_OWNER_IDS only seeds on first launch; after that manage via /addowner
# 4. Run schema first: paste supabase_schema.sql into Supabase SQL Editor
